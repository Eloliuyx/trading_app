# 是否启用后端 API 代理
# true: 前端通过 http://127.0.0.1:8000 访问 FastAPI
# false: 使用 public/out/ 下的静态 JSON 文件
VITE_USE_API=true

# 默认加载的标的（可选）
VITE_DEFAULT_CSV_URL=/data/600519.SH.csv
VITE_DEFAULT_JSON_URL=/out/600519.SH.json

# 开发端口（可选）
VITE_PORT=5173
